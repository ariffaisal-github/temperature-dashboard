# Node modules (always ignore)
node_modules/

# Environment files (security risk if pushed)
.env

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS-specific
.DS_Store

dump.rdb
appendonly.aof
*.rdb
*.aof

tests/load/tokens.json

# Optional build artifacts (if any in future)
dist/
build/
